package Model;
import acm.graphics.*;

public class Graphics {

	public static GObject drawPaddle(int x, int y) {
		return new GRect(x, y, C.PADDLE_WIDTH, C.PADDLE_HEIGHT);
	}
	public static GObject drawBall(int x, int y) {
		return new GOval(x, y, C.BALL_RAD, C.BALL_RAD);
	}
	public static GObject drawBrick(int x, int y) {
		return new GRect(x, y, C.BRICK_WIDTH, C.BRICK_HEIGHT);
	}

}
