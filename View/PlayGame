package View;

import Model.C;
import Model.Graphics;
import acm.io.IODialog;
import acm.program.*;

public class PlayGame extends GraphicsProgram {

	public void run() {
		drawtheGame();
	}

	public void drawtheGame() {
		System.out.println("Here");
		// Both still wrong size
		add(Graphics.drawPaddle((C.WINDOW_WIDTH / 2), C.WINDOW_HEIGHT - (C.WINDOW_HEIGHT / 4)));
		add(Graphics.drawBall((C.WINDOW_WIDTH / 2), C.WINDOW_HEIGHT - (C.WINDOW_HEIGHT / 4) + C.PADDLE_HEIGHT));
		// for (int i = 0; i < C.N_BRICKS; i++) {
		// Graphics.drawBrick(i * C.BRICK_WIDTH, i * C.BRICK_HEIGHT);
		// }
	}

	public void play() {
		// Hier mit nem while loop das Spiel laufen lassen
	}

	private void gameOver() {
		IODialog gameOver = new IODialog();
		gameOver.print("You Lost!");
	}

}
